export interface ButtonProps {
  text: string
  className?: string,
  onClick?: () => void,
}

export interface ButtonWithBorderProps {
  text: string;
  className?: string;
  onClick?: () => void;
}