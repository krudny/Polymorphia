name: Release

on:
  workflow_dispatch:
    inputs:
      version:
        description: "Version to release"
        required: true
        type: string

jobs:
  placeholder:
    runs-on: ubuntu-latest
    steps:
      - run: echo "This workflow must be run from the develop branch with the full implementation."